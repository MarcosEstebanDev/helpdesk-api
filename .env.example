# Copy to .env and fill in. Validated at boot by src/infrastructure/config/env.schema.ts
NODE_ENV=development
PORT=3000

# Required from Phase 2 (Postgres with Row-Level Security)
# DATABASE_URL=postgresql://helpdesk_app:helpdesk@localhost:5432/helpdesk?schema=public

# Required from Phase 5 (BullMQ + Redis)
# REDIS_URL=redis://localhost:6379
